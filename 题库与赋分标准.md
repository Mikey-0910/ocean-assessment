"""
OCEAN 人格情境测评问卷
========================
依据《大五人格.docx》编制：25 道情景式选择题，每维度 5 题。

计分规则：
- 每题 A=4 分，B=3 分，C=2 分，D=1 分。
- 若选项均不符合，用户可选 E 填写开放说明，由 LLM 按《AI 选项评分标准》
  严格版六步法（情境锚定→语义相似度加权平均→关键词强度微调→质量标记→置信度→输出反馈）评分，
  支持 0.5 分精度。
- 各维度 5 题相加，总分范围 5–20 分。

使用方式：
    python 评估与维度分数.py
    python 评估与维度分数.py --demo

依赖：
    pip install openai matplotlib numpy
"""

from __future__ import annotations

import json
import os
from dataclasses import dataclass, field
from typing import Callable, Dict, List, Optional, Tuple

# ---------------------------------------------------------------------------
# 0. 配置：可在这里替换为你实际使用的 LLM 调用方式
# ---------------------------------------------------------------------------

LLM_API_KEY = os.getenv("OPENAI_API_KEY", "")
LLM_BASE_URL = os.getenv("OPENAI_BASE_URL", "https://api.openai.com/v1")
LLM_MODEL = os.getenv("LLM_MODEL", "gpt-4o-mini")


# ---------------------------------------------------------------------------
# 1. 数据模型
# ---------------------------------------------------------------------------

@dataclass
class Option:
    """一道题的选项。"""
    label: str          # A/B/C/D
    text: str           # 选项文本
    score: int          # 该选项对应原始分（A=4，B=3，C=2，D=1）


@dataclass
class Question:
    """一道评估题。"""
    id: int
    text: str                           # 题干（情景描述）
    dimension: str                      # O / C / E / A / N
    options: List[Option]
    open_text_prompt: str               # 当用户选"都不符合"时的引导语


@dataclass
class Answer:
    """用户对一道题的回答。"""
    question_id: int
    is_open_text: bool = False
    choice_score: Optional[float] = None   # 选择题原始得分（1-4，支持 0.5 分）
    open_text: Optional[str] = None        # 开放文本内容
    open_text_score: Optional[float] = None  # LLM 按评分标准给出的最终得分（1-4，支持 0.5 分）
    open_text_reason: Optional[str] = None   # 评分理由（匹配选项、微调、异常值等）
    quality_flag: Optional[str] = None       # valid / weak_match / boundary / off_topic / contradictory
    confidence: Optional[float] = None       # 0.0-1.0

    def final_score(self) -> Optional[float]:
        """返回该题对维度的原始得分（1-4）。"""
        if self.is_open_text:
            return self.open_text_score
        return self.choice_score


@dataclass
class DimensionResult:
    """单个维度的评估结果。"""
    name: str
    total_score: float  # 维度总分 5-20
    avg_score: float    # 维度均分 1-4
    level: str          # 高/中/低
    description: str
    evidence: List[str] = field(default_factory=list)
    quality_note: str = ""  # 数据质量提示


@dataclass
class Report:
    """完整评估报告。"""
    dimension_results: Dict[str, DimensionResult]
    ocean_order: List[str] = field(default_factory=lambda: ["O", "C", "E", "A", "N"])
    open_text_summary: str = ""
    cross_dim_notes: Dict[str, str] = field(default_factory=dict)

    def to_text(self) -> str:
        lines = [
            "=" * 52,
            "OCEAN 人格情境测评报告",
            "=" * 52,
            "计分说明：每题 A=4、B=3、C=2、D=1；开放文本按 AI 评分标准严格版六步法可评 0.5 分精度；每维度 5 题总分 5–20。",
            "",
        ]
        for d in self.ocean_order:
            r = self.dimension_results[d]
            lines.append(f"【{DIMENSION_LABELS[d]} ({d})】")
            lines.append(f"  维度总分：{r.total_score:.2f} / 20  （均分 {r.avg_score:.2f} / 4.0）— {r.level}")
            lines.append(f"  解读：{r.description}")
            if r.quality_note:
                lines.append(f"  质量提示：{r.quality_note}")
            if r.evidence:
                lines.append("  关键证据：")
                for ev in r.evidence:
                    lines.append(f"    - {ev}")
            lines.append("")
        if self.open_text_summary:
            lines.append("开放文本使用情况：")
            lines.append(self.open_text_summary)
            lines.append("")
        if self.cross_dim_notes:
            lines.append("跨题一致性提示：")
            for d, note in self.cross_dim_notes.items():
                lines.append(f"  - {DIMENSION_LABELS[d]}：{note}")
            lines.append("")
        lines.append("=" * 52)
        return "\n".join(lines)


# ---------------------------------------------------------------------------
# 2. 题库（25 道情景选择题）
# ---------------------------------------------------------------------------

DIMENSION_LABELS = {
    "O": "开放性 Openness",
    "C": "尽责性 Conscientiousness",
    "E": "外向性 Extraversion",
    "A": "宜人性 Agreeableness",
    "N": "神经质 Neuroticism",
}

DIMENSION_DESCRIPTIONS = {
    "O": {
        "高": "你对新事物充满好奇，乐于探索未知，喜欢抽象思考与艺术体验。",
        "中": "你既愿意尝试新鲜事物，也会兼顾实际与舒适。",
        "低": "你偏好熟悉、具体、可预期的事物，做决定更重经验而非想象。",
    },
    "C": {
        "高": "你自律、有条理、目标导向强，注重细节并信守承诺。",
        "中": "你能在规划和灵活之间取得平衡，基本能按时完成任务。",
        "低": "你更随性、灵活，对计划和细节的关注相对较少。",
    },
    "E": {
        "高": "你外向活跃，从社交和人群中获得能量，喜欢表达与互动。",
        "中": "你既能享受社交，也需要独处恢复精力。",
        "低": "你偏内向，喜欢安静、独处或小圈子深度交流。",
    },
    "A": {
        "高": "你待人友善、富有同情心，乐于合作并愿意信任他人。",
        "中": "你在坚持自我与照顾他人之间比较平衡。",
        "低": "你更理性、直接，做决定时优先考虑逻辑而非人际和谐。",
    },
    "N": {
        "高": "你情绪体验强烈，容易焦虑、敏感，情绪波动较大。",
        "中": "你多数时候情绪稳定，但在压力下也会出现明显波动。",
        "低": "你情绪稳定、抗压能力强，面对挫折能较快恢复平静。",
    },
}

DIMENSION_ANCHORS = {
    "O": """4 分锚点：主动探索未知，享受新奇体验，喜欢抽象思辨，对陌生事物天然好奇。
3 分锚点：愿意尝试新事物，但不排斥熟悉的方案，在新鲜与安全之间保持开放。
2 分锚点：更偏好熟悉和可预期的事物，对变化持观望态度，需要有足够理由才会尝试。
1 分锚点：回避新鲜和不确定，坚持已知和常规，对变化有明显抵触。""",
    "C": """4 分锚点：自发制定计划并严格执行，追求卓越和完美交付，信守承诺，自我驱动。
3 分锚点：基本有条理，能按时完成任务，偶尔灵活调整但不影响整体质量。
2 分锚点：倾向于随性而为，计划执行力一般，对细节关注不多，需要外部提醒。
1 分锚点：明显拖延，不拘小节，对承诺较随意，最后期限才能推动行动。""",
    "E": """4 分锚点：从社交中获得能量，喜欢成为焦点，主动发起对话和活动，乐于表达。
3 分锚点：能享受社交也能独处，在社交场合中适度表达，不排斥成为焦点但不主动寻求。
2 分锚点：偏好安静和小圈子，在社交中较被动，需要独处恢复能量。
1 分锚点：回避社交场合，需要大量独处时间，在人群中感到消耗而非充电。""",
    "A": """4 分锚点：优先考虑他人感受，高度共情，为维护关系和谐愿意退让，本能信任他人。
3 分锚点：在坚持自我和照顾他人之间保持平衡，友善但有底线。
2 分锚点：更重逻辑和原则，有时显得直接，对陌生人的善意保持警惕。
1 分锚点：优先自身利益，不惧冲突，较难信任他人，认为“好心往往被利用”。""",
    "N": """4 分锚点：情绪体验强烈且持久，容易焦虑、担忧和反复思考，小事也能引起较大波动。
3 分锚点：面对压力有明显波动，但随时间能逐渐平复，偶尔会多想但能拉回来。
2 分锚点：多数时候情绪稳定，压力下有反应但不持久，能较快恢复平静。
1 分锚点：情绪稳定，抗压能力突出，几乎不因小事波动，快速恢复平静。""",
}


def opts(a: str, b: str, c: str, d: str) -> List[Option]:
    """按 A=4、B=3、C=2、D=1 快速生成选项。"""
    return [
        Option("A", a, 4),
        Option("B", b, 3),
        Option("C", c, 2),
        Option("D", d, 1),
    ]


QUESTION_BANK: List[Question] = [
    Question(1, "【旅行规划】即将到来的长假，朋友提议去一个你从未去过且文化差异很大的国家旅行，你的第一反应是？", "O",
             opts(
                 "非常兴奋，立刻开始研究当地的风土人情，并计划去探索那些鲜为人知的角落。",
                 "比较感兴趣，愿意去尝试，但可能会选择一些热门景点，确保行程安全舒适。",
                 "有些犹豫，觉得去陌生的地方太麻烦，担心语言不通或饮食不习惯。",
                 "不太想去，更愿意去一个以前去过的熟悉度假村，那样更省心、更放松。"
             ), "请描述你面对全新、陌生体验时的典型反应。"),
    Question(2, "【艺术展览】路过一家美术馆，正在展出一组风格抽象、晦涩难懂的现代艺术展，你会？", "O",
             opts(
                 "走进去仔细欣赏，即使看不懂也会努力去感受其中的情绪和创意，并觉得很有趣。",
                 "进去快速浏览一圈，看看有没有好看的色彩或构图，主要为了打发时间。",
                 "站在门口犹豫，看了几眼觉得看不明白，觉得这类东西离自己生活太远。",
                 "直接略过，觉得这是乱涂乱画，完全是浪费时间和金钱。"
             ), "请说明你对抽象艺术或陌生事物的态度。"),
    Question(3, "【学习平台】学校引入了一套全新的、使用方式变化很大的学习管理系统（如在线选课、提交作业、查成绩一体化平台），你需要从头学习，你会？", "O",
             opts(
                 "充满热情，主动挖掘新系统的高级功能，甚至思考如何用它优化现有流程。",
                 "按要求学习，尽快掌握基本操作，以便能继续完成选课、交作业、查成绩等学习任务。",
                 "略有抵触，觉得旧的系统用得挺好，为什么要变，只在不得不换时才勉强学习。",
                 "感到厌烦，私下里抱怨不断，试图寻找各种理由推迟使用新系统。"
             ), "请描述你面对学习中重大变化时的反应。"),
    Question(4, "【电影选择】周末晚上选电影，你面前有一部逻辑烧脑的科幻片和一部情节简单的喜剧片，你倾向于？", "O",
             opts(
                 "毫不犹豫选科幻片，喜欢那种脑洞大开、引发对未来和人性思考的感觉。",
                 "可能会选科幻片，只要评价不错就试试，但若太累也会考虑喜剧。",
                 "倾向于选喜剧片，看电影就是为了放松，不想动脑子去理解复杂的世界观。",
                 "坚决选喜剧片，甚至可能是一部看过的老片，不喜欢那种不切实际的幻想。"
             ), "请说明你偏好的电影或阅读类型。"),
    Question(5, "【观点讨论】在饭桌上，大家开始讨论一个极具争议的社会伦理问题（如AI伦理），你会？", "O",
             opts(
                 "积极参与，提出一个独特的、甚至挑战常理的观点，并享受思想的碰撞。",
                 "适度加入，倾听大家的意见，发表一些主流或中立的看法。",
                 "保持安静，觉得这些话题太虚，不如聊聊生活琐事实在。",
                 "感到反感，认为谈论这些没有定论的事情毫无意义，甚至试图转移话题。"
             ), "请描述你面对抽象或争议性话题时的倾向。"),

    Question(6, "【面对deadline】老师布置了一个两周后截止的重要课程项目（如课程大作业、小组报告），你会？", "C",
             opts(
                 "立即制定详细计划，每天按进度推进，争取提前两天完成以留出检查时间。",
                 "尽早开始，前一周做大部分工作，最后一周用来修改和完善。",
                 "先放一放，玩几天再说，等到最后一周开始熬夜赶工。",
                 "拖延到最后，直到截止前一晚通宵突击，能交差就行。"
             ), "请描述你管理长期任务的方式。"),
    Question(7, "【桌面环境】你的书桌或书架现在的状态是？", "C",
             opts(
                 "井井有条，所有物品分类摆放，文件按顺序归档，甚至连笔筒都朝向一致。",
                 "基本整洁，偶尔会有几张纸乱放，但大体上能找到需要的东西。",
                 "有点凌乱，堆放了不少杂物，找东西经常需要翻一阵子。",
                 "非常混乱，文件、零食、电线混在一起，常常找不到东西在哪里。"
             ), "请描述你保持整洁或处理琐事的习惯。"),
    Question(8, "【购物清单】你去超市买东西之前，通常会？", "C",
             opts(
                 "列出详细清单，甚至画好超市的路线图，严格执行清单，绝不买计划外的东西。",
                 "列个大概清单，写下必须要买的，看到如果需要且价格合适也会顺手买点别的。",
                 "大概想一下，进门时记得住要买啥，逛着逛着就凭感觉拿了。",
                 "完全随性，想买啥买啥，经常回家后发现忘了买最重要的东西。"
             ), "请描述你做计划或购物时的习惯。"),
    Question(9, "【承诺兑现】你答应朋友要在周五之前帮他一个忙，但周三突然来了个很好玩的活动邀请，你会？", "C",
             opts(
                 "拒绝邀请，既然答应了别人，必须先完成任务，诚信最重要。",
                 "调整安排，确保先把帮朋友的事情做完，如果时间来得及再去活动。",
                 "试图兼顾，去活动，但带着焦虑，想着晚上回来熬夜帮朋友做。",
                 "选择活动，心想反正周五还没到，玩回来再说，或者找借口推迟给朋友。"
             ), "请描述你在面对承诺与诱惑时的选择。"),
    Question(10, "【学习细节】你在撰写一份课程论文/实验报告，发现里面有一个错别字，但不太明显，你会？", "C",
             opts(
                 "立刻修正，并检查全文是否有类似错误，追求完美的交付成果。",
                 "顺手改掉，觉得有错别字不好看，改了比较安心。",
                 "犹豫一下，如果时间充裕就改，如果赶时间就算了。",
                 "置之不理，觉得内容没问题就行，没必要在这些细节上纠缠。"
             ), "请描述你对学习细节的态度。"),

    Question(11, "【周末充电】经过了一周高压且忙碌的学习/考试周，周五晚上你感到精疲力尽，你希望如何度过周末？", "E",
             opts(
                 "呼朋唤友，去KTV、聚餐或参加派对，在热闹的人群和欢笑声中满血复活。",
                 "社交一两次，比如约个朋友吃顿饭，或者和家人一起出去玩。",
                 "宅家休息，看看电视、睡个懒觉，不想出门社交，也不想见太多人。",
                 "彻底独处，关掉手机，一个人看书、散步或发呆，享受完全不被打扰的宁静。"
             ), "请描述你疲惫时偏好的恢复方式。"),
    Question(12, "【陌生聚会】你被拉进一个全是陌生人的社交场合（如社团招新、新生联谊），你的状态通常是？", "E",
             opts(
                 "如鱼得水，主动走向不同的人自我介绍，热情地开启话题，很快成为焦点。",
                 "适应良好，如果有人来搭话会很愉快地交流，也会尝试加入一两个圈子。",
                 "略显拘谨，主要呆在熟悉的人身边，或者找个角落玩手机，偶尔看看周围。",
                 "坐立难安，感到极度不自在，找借口尽快离开，或者全程沉默不语。"
             ), "请描述你在陌生社交场合的典型表现。"),
    Question(13, "【表达方式】在课堂讨论或小组汇报中，你有一个很好的想法，你会？", "E",
             opts(
                 "大声且快速地表达出来，一边说一边用手势，情绪饱满，希望所有人都听到。",
                 "清晰地阐述，等待合适的时机发言，逻辑清楚，声音适中。",
                 "小声地提议，或者先私发给旁边的同学，确认没问题了再说。",
                 "写在文档里，不喜欢口头争辩或展示，更倾向于用文字表达。"
             ), "请描述你在团队中表达观点的方式。"),
    Question(14, "【成为焦点】在生日派对上，大家突然为你唱生日歌并盯着你，你的感觉是？", "E",
             opts(
                 "非常享受，甚至可能会趁机讲个笑话或做个搞怪动作，感觉很棒。",
                 "有点害羞但开心，笑着感谢大家，觉得这是甜蜜的时刻。",
                 "有些尴尬，希望快点结束，不知道手该往哪里放。",
                 "极度不适，如果可能的话想钻到地缝里，讨厌这种被众人注视的感觉。"
             ), "请描述你成为众人焦点时的感受。"),
    Question(15, "【沟通偏好】你更倾向于哪种沟通方式来解决学习或生活中的问题？", "E",
             opts(
                 "面对面或电话，觉得文字太冷淡，必须听到声音或看到人才能高效沟通。",
                 "即时通讯软件（语音/文字），方便快捷，也能感受到对方的情绪。",
                 "邮件或留言，给自己留出思考和整理的时间，不喜欢被打扰。",
                 "尽量避免沟通，最好能自己查资料解决，或者写信条，不想有实时互动。"
             ), "请描述你偏好的沟通方式。"),

    Question(16, "【朋友求助】一位朋友在深夜向你倾诉他学习上的烦恼（如考试失利、社团矛盾），但其实这个烦恼主要是因为他自己的失误造成的，你会？", "A",
             opts(
                 "温柔安慰，哪怕是他错了，也先表示理解他的痛苦，不想让他更难过。",
                 "表示同情，但也委婉地指出问题所在，希望能帮到他。",
                 "理性分析，直接告诉他哪里做错了，应该怎么补救，觉得安慰没用。",
                 "直言不讳，甚至有点生气地批评他太蠢了，‘早就告诉过你会这样’。"
             ), "请描述你安慰或帮助他人时的典型方式。"),
    Question(17, "【利益冲突】在小组作业中，你和队友在方案上产生了严重分歧，且你的方案明显对作业成绩更有利，你会？", "A",
             opts(
                 "主动退让，为了维护团队和谐，假装同意他的方案，不想发生争吵。",
                 "寻求折中，建议把双方的意见融合一下，哪怕那样会降低方案的质量。",
                 "据理力争，坚持我的观点，尝试说服他接受我的方案，但语气保持专业。",
                 "强硬到底，直接向老师或组长汇报，或者毫不留情地反驳他的观点，一定要赢。"
             ), "请描述你处理人际分歧的风格。"),
    Question(18, "【捡到财物】你在商场捡到了一个装有贵重物品的钱包，但周围没人，你会？", "A",
             opts(
                 "毫不犹豫，立刻送到失物招领处，甚至想办法联系失主，担心失主着急。",
                 "会交上去，觉得这是应该做的，虽然可能会有点麻烦。",
                 "犹豫片刻，看一眼里面有多少钱，虽然最终会交，但心里会有波动。",
                 "据为己有，觉得这是运气，或者这是‘谁让他不小心’的意外之财。"
             ), "请描述你在类似情境下的第一反应。"),
    Question(19, "【电影情节】看电影时，主角为了拯救爱狗而牺牲了自己宝贵的逃生机会，你会？", "A",
             opts(
                 "感动落泪，觉得这份感情太珍贵了，完全沉浸在悲伤的情绪中。",
                 "有些感动，觉得虽然不理智，但能理解这种情感。",
                 "觉得不值，认为人比狗重要，这种做法太冲动、太不理性了。",
                 "感到可笑，认为这是编剧编造的煽情桥段，毫无逻辑可言。"
             ), "请描述你看到感人情节时的反应。"),
    Question(20, "【信任初识】刚认识一位新同学，他主动提出要帮你带早餐，你会？", "A",
             opts(
                 "充满感激，觉得他人真好，立刻接受并表示感谢。",
                 "礼貌接受，觉得这是一个友好的表示，下次记得还回去就行。",
                 "保持警惕，觉得无功不受禄，婉言拒绝或怀疑他别有用心。",
                 "直接拒绝，觉得不喜欢欠人人情，也不相信陌生人的善意。"
             ), "请描述你对陌生人善意的态度。"),

    Question(21, "【突发事件】正准备出门时，突然发现校园卡/身份证找不到了，而你有一场非常重要的考试不能迟到，你会？", "N",
             opts(
                 "极度恐慌，心跳加速，手心冒汗，感到天都要塌了，完全乱了阵脚。",
                 "非常焦急，一边抱怨一边翻箱倒柜，情绪变得很糟糕。",
                 "有些烦躁，深吸几口气，告诉自己冷静，快速回想最后一次放哪。",
                 "保持冷静，情绪几乎没有波动，迅速制定备用方案（如找辅导员开证明、借同学证件）。"
             ), "请描述你面对突发压力时的情绪反应。"),
    Question(22, "【批评反馈】老师把你叫到办公室，严肃地指出了你作业/论文中的一部分数据错误，你会？", "N",
             opts(
                 "深受打击，觉得老师是不是对我很失望，我会不会因此挂科，或者这门课要重修，为此难过好几天。",
                 "情绪低落，虽然知道错了，但心里很委屈，觉得自己之前那么努力白费了。",
                 "有些自责，但能调节过来，想着下次注意就好，不会太往心里去。",
                 "坦然接受，情绪不受影响，专注于错误本身，改正后就忘了这事。"
             ), "请描述你面对批评时的情绪反应。"),
    Question(23, "【健康疑病】最近经常头痛，上网搜索了一下，发现很多重病都有头痛的症状，你会？", "N",
             opts(
                 "灾难化想象，坚信自己得了绝症，甚至开始写遗嘱，焦虑得睡不着觉。",
                 "非常担忧，坐立难安，立刻想去医院做全身检查，反复确认。",
                 "适度注意，觉得多休息就好，如果还没好再去看看医生。",
                 "不在意，觉得可能只是没睡好，过两天自然就好了，完全不担心。"
             ), "请描述你对身体不适时的担忧程度。"),
    Question(24, "【公开演讲】需要在一二百人的课堂汇报/课程答辩/社团活动现场发言，上台前一分钟，你会？", "N",
             opts(
                 "濒临崩溃，大脑一片空白，甚至想呕吐或逃跑。",
                 "紧张焦虑，心跳很快，一直在想‘万一忘词了怎么办’。",
                 "有点紧张，深呼吸几次，这种紧张感反而让自己更集中精神。",
                 "甚至兴奋，把它当成一个展示自己的机会，心态平和，自信满满。"
             ), "请描述你在重要公开场合前的状态。"),
    Question(25, "【情绪恢复】刚经历了一件很倒霉的事情（如被诈骗了钱），现在的你会？", "N",
             opts(
                 "深陷泥潭，这件事会在脑子里转很久，反复回想细节，几天甚至几周都处于愤怒或抑郁中。",
                 "需要较长时间，虽然试图做别的事，但还是会时不时感到难受，需要一周左右缓解。",
                 "较快恢复，可能会郁闷一晚上，但睡一觉或者找朋友吐槽后，第二天基本能回归正常。",
                 "迅速翻篇，能很快接受现实（‘钱没了再赚’），几乎不影响到当下的心情和生活节奏。"
             ), "请描述你经历挫折后的恢复速度。"),
]

QUESTION_BY_ID: Dict[int, Question] = {q.id: q for q in QUESTION_BANK}


# ---------------------------------------------------------------------------
# 3. LLM 评分器（可插拔）
# ---------------------------------------------------------------------------

class LLMScorer:
    """
    按《AI 选项评分标准》严格版六步法对开放文本进行 1–4 分评分（支持 0.5 分精度）。
    默认使用 OpenAI 兼容接口；如果未配置 API key，则回退到命令行交互。

    评分标准六步法：
        1. 情境锚定：锁定题目、四个预设选项及维度行为锚点表。
        2. 语义相似度加权平均：用户文本与 A/B/C/D 选项做语义比对，得到 base_score。
        3. 关键词强度微调：三级强度 × 权重累积，含否定前缀反转与语境方向判定。
        4. 质量标记：valid / weak_match / boundary / off_topic / contradictory。
        5. 置信度：输出 0.0–1.0 的 confidence。
        6. 输出反馈：返回 final_score、quality_flag、confidence、reason。
    """

    # 加分词：行为强度高于基准选项，倾向高分
    INTENSIFY_STRONG = [
        "甚至", "超额", "必须", "立刻", "绝对", "极度享受", "完全无法忍受",
        "毫无疑问", "完全", "极其", "总是", "一定",
    ]
    INTENSIFY_MEDIUM = ["非常", "特别", "格外"]
    INTENSIFY_WEAK = ["很喜欢", "很期待", "很享受", "很感兴趣", "比较喜欢", "倾向于"]

    # 减分词：行为强度低于基准选项，或带有勉强/抗拒情绪，倾向低分
    WEAKEN_STRONG = [
        "抗拒", "讨厌", "不喜欢", "懒得", "完全不", "绝不", "打死也不", "被迫", "不得已",
    ]
    WEAKEN_MEDIUM = [
        "偶尔", "尽量", "看心情", "不得不", "有点", "虽然", "但是", "有时",
        "可能", "大概", "勉强", "不太想", "不情愿",
    ]
    WEAKEN_WEAK = ["不太确定"]

    # 混合词：表达摇摆或情境依赖，出现即触发 ambiguity=true
    MIXED_WORDS = [
        "有时候", "看情况", "一半一半", "视情况而定", "时好时坏", "不一定",
        "摇摆不定", "忽上忽下",
    ]

    def __init__(
        self,
        api_key: Optional[str] = None,
        base_url: Optional[str] = None,
        model: Optional[str] = None,
    ):
        self.api_key = api_key or LLM_API_KEY
        self.base_url = base_url or LLM_BASE_URL
        self.model = model or LLM_MODEL
        self._client = None

    def _get_client(self):
        if self._client is None:
            try:
                import openai
            except ImportError as exc:
                raise ImportError("使用 OpenAI 接口需要安装 openai：pip install openai") from exc
            self._client = openai.OpenAI(api_key=self.api_key, base_url=self.base_url)
        return self._client

    def score_open_text(
        self,
        question: Question,
        user_text: str,
    ) -> Tuple[float, str, str, float]:
        """返回 (1-4 维度最终得分, 评分理由, quality_flag, confidence)。"""
        if not self.api_key or self.api_key in ("YOUR_API_KEY", ""):
            return self._fallback_manual(question, user_text)

        prompt = self._build_grading_prompt(question, user_text)
        system_msg = (
            "你是一位专业的人格心理学评估助手。请严格按照《AI 选项评分标准》严格版六步法"
            "（情境锚定→语义相似度加权平均→关键词强度微调→质量标记→置信度→输出反馈）"
            "对用户的开放回答评分。分数范围 1.0–4.0，支持 0.5 分精度。"
            "必须输出 quality_flag 和 confidence。只输出 JSON 格式，不要输出任何解释性文字。"
        )
        try:
            client = self._get_client()
            resp = client.chat.completions.create(
                model=self.model,
                messages=[
                    {"role": "system", "content": system_msg},
                    {"role": "user", "content": prompt},
                ],
                temperature=0.2,
                response_format={"type": "json_object"},
            )
            content = resp.choices[0].message.content
            data = json.loads(content)
            final_score = float(data.get("final_score", data.get("base_score", 2.5)))
            reason = data.get("reason", "")
            best_match = data.get("best_match", "")
            similarities = data.get("similarities", {})
            quality_flag = data.get("quality_flag", "valid")
            confidence = float(data.get("confidence", 0.7))
            adjustment = float(data.get("adjustment", 0.0))
            detail = self._format_reason(
                best_match, similarities, adjustment, quality_flag, confidence, reason
            )
            return (
                max(1.0, min(4.0, final_score)),
                detail,
                quality_flag,
                max(0.0, min(1.0, confidence)),
            )
        except Exception as exc:
            print(f"[LLM 调用失败] {exc}，转入手动评分。")
            return self._fallback_manual(question, user_text)

    def _build_grading_prompt(self, question: Question, user_text: str) -> str:
        dim_label = DIMENSION_LABELS.get(question.dimension, question.dimension)
        opts = question.options
        anchors = DIMENSION_ANCHORS.get(question.dimension, "")
        return (
            f"=== 第一步：情境锚定 ===\n"
            f"题目编号：第 {question.id} 题\n"
            f"评估维度：{dim_label}\n"
            f"题目情境：{question.text}\n"
            f"四个预设选项及对应原始分：\n"
            f"A（4分）：{opts[0].text}\n"
            f"B（3分）：{opts[1].text}\n"
            f"C（2分）：{opts[2].text}\n"
            f"D（1分）：{opts[3].text}\n\n"
            f"=== 维度行为锚点表（用于 weak_match 或异常值推断） ===\n"
            f"{anchors}\n\n"
            f"=== 第二步：语义相似度计算与 base_score ===\n"
            f"1. 在计算相似度之前，请先忽略选项的排列顺序（A/B/C/D 仅为编号，不代表偏好方向），"
            f"仅基于语义内容进行判断。不要因为 A 排在第一位就倾向给出更高的相似度。\n"
            f"2. 估算用户回答与 A/B/C/D 四个选项的语义相似度 sim_A、sim_B、sim_C、sim_D（0%-100%）。\n"
            f"3. 若四项相似度之和 ≥ 1%，计算加权平均分：\n"
            f"   base_score = (sim_A×4 + sim_B×3 + sim_C×2 + sim_D×1) / (sim_A+sim_B+sim_C+sim_D)\n"
            f"   若四项相似度之和 < 1%，直接判定为 off_topic，final_score = 2.5。\n\n"
            f"相似度门槛：\n"
            f"- 最高相似度 < 35% → weak_match：结合维度行为锚点推断 1.0–4.0 的分数，confidence < 0.5\n"
            f"- 最高 ≥ 80% 且其余 < 20% → strong_match：跳过关键词微调，直接以 weighted_base_score 为 final_score，"
            f"quality_flag=valid，confidence=0.9\n"
            f"- 最高与次高之差 < 10pp → boundary：按竞争规则取中位数或平均值\n"
            f"- 四项相似度两两之差均 < 10pp → ambiguity：base_score = 2.5\n\n"
            f"=== 第三步：关键词强度微调（仅非 strong_match 时执行） ===\n"
            f"加分词-强（weight=1.5）：{', '.join(self.INTENSIFY_STRONG)}\n"
            f"加分词-中（weight=1.0）：{', '.join(self.INTENSIFY_MEDIUM)}\n"
            f"加分词-弱（weight=0.5）：{', '.join(self.INTENSIFY_WEAK)}\n"
            f"减分词-强（weight=1.5）：{', '.join(self.WEAKEN_STRONG)}\n"
            f"减分词-中（weight=1.0）：{', '.join(self.WEAKEN_MEDIUM)}\n"
            f"减分词-弱（weight=0.5）：{', '.join(self.WEAKEN_WEAK)}\n"
            f"混合词（出现即触发 ambiguity=true）：{', '.join(self.MIXED_WORDS)}\n\n"
            f"否定前缀反转规则：检查否定结构（不、没、无、非、别、甭、莫、不太、不是很、"
            f"不怎么、并不、从未、绝不、决不、不要、不能、别去、不准）。"
            f"如果关键词前有否定前缀，反转该关键词的加减方向并降一级权重。"
            f"例如\"不讨厌\"应识别为加分词-弱（weight=0.5）。从最长复合否定开始匹配。双重否定还原为正向。\n\n"
            f"语境方向判定规则：先由 base_score 确定核心行为方向（>2.5 为高分倾向，<2.5 为低分倾向）。"
            f"如果关键词修饰的是低分方向行为，则关键词加减方向反转。"
            f"例如 base_score=1.3，\"绝对不去\"中\"绝对\"强化低分方向 → 从加分词反转为减分词。\n\n"
            f"混合词优先规则：任何混合词匹配 → ambiguity=true，该词不再计入 intensity_boost 或 weaken_penalty。\n\n"
            f"微调计算公式：\n"
            f"intensity_boost = Σ(加分词出现次数 × 该级 weight)  // 先执行否定反转与语境方向判定\n"
            f"weaken_penalty  = Σ(减分词出现次数 × 该级 weight)\n"
            f"net_adjustment  = min(1.5, intensity_boost) - min(1.5, weaken_penalty)\n"
            f"final_score     = base_score + net_adjustment\n"
            f"最终结果限制在 [1.0, 4.0]。\n\n"
            f"=== 第四步：质量标记（quality_flag） ===\n"
            f"| 标记 | 触发条件 |\n"
            f"| valid | 回答与情境相关，语义清晰，最高相似度 ≥ 35% |\n"
            f"| weak_match | 最高相似度 < 35%，但用户确实在认真回答 |\n"
            f"| boundary | 最高与次高相似度之差 < 10pp |\n"
            f"| off_topic | 答非所问、明显敷衍、字数 < 5 个有意义汉字 |\n"
            f"| contradictory | 文本内部自相矛盾，或关键词密度 > 40% |\n\n"
            f"=== 第五步：置信度（confidence） ===\n"
            f"输出 0.0–1.0 的置信度。≥0.7 可信，0.5–0.7 存疑，<0.5 不可信。\n"
            f"置信度调整：strong_match +0.1；boundary -0.1；有效汉字数 ≥15 且关键词密度 <10% +0.1；关键词密度 >40% -0.2。\n"
            f"最终 confidence 限制在 [0.0, 1.0]。\n\n"
            f"=== 用户开放回答 ===\n"
            f"{user_text}\n\n"
            f"=== 输出格式 ===\n"
            f"请只输出如下 JSON（不要 Markdown 代码块）：\n"
            f'{{\n'
            f'  "base_score": float,          // 加权平均后的基础分\n'
            f'  "best_match": "A/B/C/D",      // 最匹配选项\n'
            f'  "similarities": {{"A": float, "B": float, "C": float, "D": float}},  // 0-100\n'
            f'  "final_score": float,         // 最终得分（1.0-4.0，支持0.5）\n'
            f'  "quality_flag": "valid|weak_match|boundary|off_topic|contradictory",\n'
            f'  "confidence": float,          // 0.0-1.0\n'
            f'  "reason": str                 // 简要说明评分依据\n'
            f'}}'
        )

    @staticmethod
    def _format_reason(
        best_match: str,
        similarities: Dict[str, float],
        adjustment: float,
        quality_flag: str,
        confidence: float,
        reason: str,
    ) -> str:
        """把 LLM 返回的结构化信息格式成易读的理由。"""
        parts = []
        if best_match:
            parts.append(f"最匹配选项：{best_match}")
        if similarities and isinstance(similarities, dict):
            sim_str = ", ".join(
                f"{k}:{int(v)}%" for k, v in similarities.items() if isinstance(v, (int, float))
            )
            if sim_str:
                parts.append(f"相似度：{sim_str}")
        if adjustment:
            sign = "+" if adjustment > 0 else ""
            parts.append(f"微调：{sign}{adjustment}")
        parts.append(f"质量标记：{quality_flag}")
        parts.append(f"置信度：{confidence:.2f}")
        if reason:
            parts.append(f"判定：{reason}")
        return "；".join(parts) if parts else "LLM 评分"

    def _fallback_manual(
        self,
        question: Question,
        user_text: str,
    ) -> Tuple[float, str, str, float]:
        """无 LLM 时，让操作者手动输入分数。"""
        print(f"\n[需要手动评分] 维度：{DIMENSION_LABELS.get(question.dimension, question.dimension)}")
        print(f"题目：{question.text}")
        print(f"用户回答：{user_text}")
        while True:
            raw = input("请输入 1-4 分（支持 0.5，如 3.5）：").strip()
            try:
                score = float(raw)
                if 1.0 <= score <= 4.0 and score * 10 % 5 == 0:
                    return score, "手动评分", "valid", 1.0
            except ValueError:
                pass
            print("输入无效，请输入 1.0–4.0 之间且为 0.5 倍数的数字。")


# ---------------------------------------------------------------------------
# 4. 评估引擎
# ---------------------------------------------------------------------------

class OCEANEngine:
    def __init__(self, llm_scorer: Optional[LLMScorer] = None):
        self.llm_scorer = llm_scorer or LLMScorer()
        self.answers: Dict[int, Answer] = {}

    def collect_answers(self, input_fn: Callable[[str], str] = input) -> List[Answer]:
        """通过命令行交互收集用户答案。"""
        print("=" * 60)
        print("OCEAN 人格情境测评问卷")
        print("共 25 道题，每题 A-D 四个选项；如都不符合可选 E 填写说明。")
        print("=" * 60)

        answers: List[Answer] = []
        for q in QUESTION_BANK:
            print(f"\n第 {q.id} 题")
            print(q.text)
            for opt in q.options:
                print(f"  {opt.label}. {opt.text}")
            print("  E. 以上都不符合，我想自己说明")

            while True:
                choice = input_fn("请选择（A/B/C/D/E）：").strip().upper()
                if choice in {"A", "B", "C", "D"}:
                    opt_map = {opt.label: opt for opt in q.options}
                    ans = Answer(
                        question_id=q.id,
                        is_open_text=False,
                        choice_score=opt_map[choice].score,
                    )
                    break
                if choice == "E":
                    text = input_fn("请输入你的说明：").strip()
                    score, reason, quality_flag, confidence = self.llm_scorer.score_open_text(q, text)
                    ans = Answer(
                        question_id=q.id,
                        is_open_text=True,
                        open_text=text,
                        open_text_score=score,
                        open_text_reason=reason,
                        quality_flag=quality_flag,
                        confidence=confidence,
                    )
                    print(f"  → 评分：{score}/4，理由：{reason}")
                    break
                print("输入无效，请重新选择。")
            answers.append(ans)

        self.answers = {a.question_id: a for a in answers}
        return answers

    def compute_report(
        self,
        answers: Optional[List[Answer]] = None,
        generate_chart: bool = True,
    ) -> Report:
        """根据答案计算维度分数并生成报告。"""
        if answers is None:
            answers = list(self.answers.values())

        # 汇总到各维度（开放文本 evidence 优先显示）
        dim_scores: Dict[str, List[float]] = {d: [] for d in DIMENSION_LABELS}
        open_evidence: Dict[str, List[str]] = {d: [] for d in DIMENSION_LABELS}
        choice_evidence: Dict[str, List[str]] = {d: [] for d in DIMENSION_LABELS}
        dim_quality_flags: Dict[str, List[str]] = {d: [] for d in DIMENSION_LABELS}
        dim_confidences: Dict[str, List[float]] = {d: [] for d in DIMENSION_LABELS}
        open_text_scores_by_dim: Dict[str, List[float]] = {d: [] for d in DIMENSION_LABELS}

        for ans in answers:
            q = QUESTION_BY_ID[ans.question_id]
            score = ans.final_score()
            if score is None:
                continue
            dim_scores[q.dimension].append(score)
            if ans.is_open_text:
                reason_snippet = ans.open_text_reason or "LLM评分"
                evidence = (
                    f"Q{q.id}: 开放文本“{ans.open_text[:30]}...”→{score}分"
                    f"（{reason_snippet}）"
                )
                open_evidence[q.dimension].append(evidence)
                if ans.quality_flag:
                    dim_quality_flags[q.dimension].append(ans.quality_flag)
                if ans.confidence is not None:
                    dim_confidences[q.dimension].append(ans.confidence)
                open_text_scores_by_dim[q.dimension].append(score)
            else:
                evidence = f"Q{q.id}: 选择{ans.choice_score}分"
                choice_evidence[q.dimension].append(evidence)

        dim_evidence = {
            d: (open_evidence[d] + choice_evidence[d])[:3]
            for d in DIMENSION_LABELS
        }

        # 计算结果
        results: Dict[str, DimensionResult] = {}
        for d in DIMENSION_LABELS:
            scores = dim_scores[d]
            total = sum(scores) if scores else 12.5
            avg = total / len(scores) if scores else 2.5
            level = self._level(avg)

            # 数据质量提示
            quality_note = ""
            non_valid_flags = [f for f in dim_quality_flags[d] if f != "valid"]
            if len(non_valid_flags) >= 2:
                quality_note = "本维度数据质量较低，结果仅供参考。"

            results[d] = DimensionResult(
                name=DIMENSION_LABELS[d],
                total_score=round(total, 2),
                avg_score=round(avg, 2),
                level=level,
                description=DIMENSION_DESCRIPTIONS[d][level],
                evidence=dim_evidence[d][:3],
                quality_note=quality_note,
            )

        # 跨题一致性检测
        cross_dim_notes: Dict[str, str] = {}
        for d in DIMENSION_LABELS:
            open_scores = open_text_scores_by_dim[d]
            if len(open_scores) >= 2:
                score_range = max(open_scores) - min(open_scores)
                if score_range >= 2.0:
                    cross_dim_notes[d] = (
                        "存在较大场景差异；同一人格特质在不同情境下可能有不同表现。"
                    )

        # 开放文本总评
        open_answers = [a for a in answers if a.is_open_text]
        if open_answers:
            open_summary_lines = [
                f"用户有 {len(open_answers)} 道题使用开放说明，"
                f"主要分布在以下维度："
                f"{', '.join(sorted({QUESTION_BY_ID[a.question_id].dimension for a in open_answers}))}。"
                "这些说明已纳入对应维度总分。"
            ]
            for ans in open_answers:
                q = QUESTION_BY_ID[ans.question_id]
                flag = ans.quality_flag or "unknown"
                conf = f"{ans.confidence:.2f}" if ans.confidence is not None else "N/A"
                open_summary_lines.append(
                    f"  Q{q.id} ({q.dimension}): 得分 {ans.open_text_score}, flag={flag}, confidence={conf}"
                )
            open_summary = "\n".join(open_summary_lines)
        else:
            open_summary = "用户全部使用选项作答，无开放文本。"

        report = Report(
            dimension_results=results,
            open_text_summary=open_summary,
            cross_dim_notes=cross_dim_notes,
        )

        if generate_chart:
            self._draw_radar(results)

        return report

    @staticmethod
    def _level(avg: float) -> str:
        """维度均分 1.0–4.0 的等级划分。"""
        if avg >= 3.0:
            return "高"
        if avg >= 2.0:
            return "中"
        return "低"

    @staticmethod
    def _draw_radar(results: Dict[str, DimensionResult], save_path: str = "ocean_radar.png"):
        """绘制雷达图并保存。"""
        try:
            import matplotlib.pyplot as plt
            import numpy as np
        except ImportError:
            print("[提示] 未安装 matplotlib/numpy，跳过雷达图生成。")
            return

        labels = [DIMENSION_LABELS[d] for d in ["O", "C", "E", "A", "N"]]
        values = [results[d].total_score for d in ["O", "C", "E", "A", "N"]]
        values += values[:1]  # 闭合

        angles = np.linspace(0, 2 * np.pi, len(labels), endpoint=False).tolist()
        angles += angles[:1]

        fig, ax = plt.subplots(figsize=(6, 6), subplot_kw=dict(polar=True))
        ax.plot(angles, values, "o-", linewidth=2, color="#2E86AB")
        ax.fill(angles, values, alpha=0.25, color="#2E86AB")
        ax.set_xticks(angles[:-1])
        ax.set_xticklabels(labels, fontsize=10)
        ax.set_ylim(5, 20)
        ax.set_title("OCEAN 人格维度雷达图", fontsize=14, pad=20)
        plt.tight_layout()
        plt.savefig(save_path, dpi=150)
        print(f"[雷达图已保存] {save_path}")


# ---------------------------------------------------------------------------
# 5. 入口函数与示例
# ---------------------------------------------------------------------------

def run_assessment():
    """完整评估流程。"""
    engine = OCEANEngine()
    answers = engine.collect_answers()
    report = engine.compute_report(answers)
    print("\n" + report.to_text())
    return report


def demo_with_mock_answers():
    """不依赖交互，使用预设答案快速演示框架。"""
    print("=" * 60)
    print("【演示模式】使用预设答案生成报告")
    print("=" * 60)

    engine = OCEANEngine(llm_scorer=LLMScorer())

    # 演示开放文本按 AI 评分标准得到 0.5 分精度的样例
    mock_open_samples = {
        5: ("如果话题确实有趣，我也会提出一些不同寻常的看法，但不会特别激进。", 3.5,
            "最匹配选项：B；相似度：A:40%, B:75%, C:20%, D:5%；微调：表达温和，-0.5 → 3.5",
            "valid", 0.75),
        10: ("我会顺手改掉错别字，但不会特意全文再检查一遍。", 3.5,
             "最匹配选项：B；相似度：A:55%, B:85%, C:25%, D:5%；微调：有轻微弱化倾向，-0.5 → 3.5",
             "valid", 0.8),
        15: ("我更喜欢先发消息把问题说清楚，复杂了再约电话。", 2.5,
             "最匹配选项：C；相似度：A:10%, B:40%, C:80%, D:25%；微调：无明显强化/弱化，保持 2.5",
             "valid", 0.85),
        20: ("我会礼貌接受，但心里会有点不自在，想着下次一定要还回去。", 3.0,
             "最匹配选项：B；相似度：A:45%, B:80%, C:30%, D:5%；微调：减分词“有点不自在”，-0.5；但接受善意回升，最终 3.0",
             "valid", 0.75),
        25: ("我可能会郁闷一晚上，找朋友吐槽一下，第二天基本就没事了。", 2.5,
             "最匹配选项：C；相似度：A:5%, B:20%, C:85%, D:20%；微调：无明显强化/弱化，保持 2.5",
             "valid", 0.85),
    }

    mock = []
    for q in QUESTION_BANK:
        if q.id in mock_open_samples:
            text, score, reason, flag, conf = mock_open_samples[q.id]
            mock.append(Answer(
                question_id=q.id,
                is_open_text=True,
                open_text=text,
                open_text_score=score,
                open_text_reason=reason,
                quality_flag=flag,
                confidence=conf,
            ))
        else:
            mock.append(Answer(
                question_id=q.id,
                is_open_text=False,
                choice_score=4,
            ))
    report = engine.compute_report(mock)
    print(report.to_text())
    return report


if __name__ == "__main__":
    import sys
    if len(sys.argv) > 1 and sys.argv[1] == "--demo":
        demo_with_mock_answers()
    else:
        run_assessment()
